rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Test mode: Allow all authenticated users to read/write
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
